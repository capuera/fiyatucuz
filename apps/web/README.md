# @fiyatucuz/web

Public consumer-facing web application. Next.js App Router, TypeScript strict.

**Foundation state:** placeholder homepage. No product UI, no API integration, no styling library.

## Local run

```bash
pnpm --filter @fiyatucuz/web dev
open http://localhost:3000
```
