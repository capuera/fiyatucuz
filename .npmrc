node-linker=isolated
# React Native (Expo) and Next.js legitimately require different React versions in the
# same workspace. Strict peers cannot resolve this cleanly; relaxing it is the standard
# convention. Individual packages still enforce their own peer expectations at build time.
strict-peer-dependencies=false
auto-install-peers=true
save-workspace-protocol=rolling
