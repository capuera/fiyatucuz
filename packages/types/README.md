# @fiyatucuz/types

Foundation TypeScript types shared across backend, web, and mobile.

**Scope:** primitive value types, branded types (`Ulid`, `TenantId`), pagination shape, error envelope.

**Out of scope:** domain-specific types. Those live in the bounded context that owns them.
